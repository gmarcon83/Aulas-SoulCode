calc(); -> permite calcular valores, usar espaço entre os operadores

repeat(x, y); -> retorna valor y repetido x vezes

attr(); -> Retorna uma string com valor de um atributo

clamp(min, var, max); -> segue o tamanho var, mas limitando entre min e max